# ==============================================================================
# Example CODEOWNERS — rename it to `CODEOWNERS` to activate it.
# GitHub will automatically request review from the owners on every PR that
# touches their paths. See https://docs.github.com/articles/about-code-owners
# ==============================================================================

# Default owner for the whole repository.
* @[GITHUB_USER]

# Owners per area (examples — adjust or remove).
# docs/           @[GITHUB_USER]
# .github/        @[GITHUB_USER]
# .claude/        @[GITHUB_USER]
